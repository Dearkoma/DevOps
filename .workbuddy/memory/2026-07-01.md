# 2026-07-01 工作日志

## 实例展开面板新增容器日志查看与保存
- **需求**：实例下拉面板除了链接，还要显示 Docker/K8s 对该项目的日志，并且可以保存
- **后端**：
  - `InstanceMonitorService.java` 新增 `getContainerLogs(Long id, int tail)` 方法
    - K8s：`kubectl logs <pod> -n <ns> --tail=<tail>`，Pod 崩溃时自动回退 `--previous`
    - Docker：`docker logs --tail <tail> <containerId>`
    - 复用已有的 `kubectl()` / `docker()` ProcessBuilder 安全执行方法
  - `InstanceController.java` 新增 `GET /api/instances/{id}/logs?tail=200`
- **前端**：
  - `api.js` 新增 `getInstanceLogs(id, tail)`
  - `InstanceList.jsx` 展开面板新增日志区域：
    - 查看日志按钮 → 调用 API 获取日志
    - 行数选择（100/200/500/1000行）
    - 刷新按钮、保存到文件按钮（下载 .txt）、关闭按钮
    - 日志以深色终端风格 `<pre>` 显示，maxHeight 400px 可滚动
  - 新增 state: `logsData`, `logsLoading`, `showLogsId`
  - 新增回调: `handleViewLogs`, `handleSaveLogs`, `handleCloseLogs`
  - 通过 `shared` 对象传递到所有三个视图组件（AllInstancesView/DockerView/K8sView）
- **提交**: `da14bd2`

## 修复 K8s 日志获取报 NotFound
- **问题**：`pods "proj-xxx-7f55c7956f-qpwm2" not found` —— 数据库存的 Pod 名已过期（Deployment 重建 Pod 后 hash 后缀变了）
- **修复**：
  - 新增 `resolveK8sPodNameAllPhases(ns, searchTerm)`：不限 Running 状态，能找到 CrashLoopBackOff/Failed 的 Pod
  - `collectK8sLogs()` 改为始终实时查询当前 Pod 名，不再盲信存储的 `k8sPodName`
  - 解析成功后自动更新数据库 `k8sPodName` 字段（自愈）
- **提交**: `cf48500`

## 修复 K8s 日志搜索词用错字段
- **问题**：上一版修复用了 `projectName`（值=`DevOps`）搜索 Pod，但 Pod 名是 `proj-1782825767454-xxx`，完全匹配不到
- **修复**：搜索词改为 `instanceName` 去掉 `-k8s`/`-docker` 后缀（`proj-1782825767454-k8s` → `proj-1782825767454`）
- **实际 Pod**: `proj-1782825767454-6c5859b554-4fnp4`，前缀 `proj-1782825767454-` 能匹配
- **提交**: `6a52a88`

## 修复 K8s 重启/停止/删除用 Pod 名当 Deployment 名
- **问题**：`kubectl rollout restart deployment/proj-xxx-7f55c7956f-qpwm2` 失败，因为 Deployment 名是 `proj-xxx`，不是 Pod 名
- **修复**：
  - 新增 `resolveK8sDeploymentName(inst)`：instanceName 去 `-k8s`/`-docker` 后缀 → Deployment 名
  - `restartK8sInstance`：`kubectl rollout restart deployment/<depName> -n <ns>`
  - `stopK8sInstance`：`kubectl scale deployment/<depName> --replicas=0 -n <ns>`
  - `deleteK8sResources`：`kubectl delete deployment <depName> -n <ns>`
  - 全部改用 `kubectl()` ProcessBuilder 传参数组
- **提交**: `ae41b64`

## 修复日志功能不显示 + 恢复停止的实例
- **问题**：用户之前点了"停止"，Deployment replicas 被缩到 0，Pod 消失，日志获取报 NotFound
- **修复**：
  - `resolveK8sPodNameAllPhases` 改用 `kubectl()` ProcessBuilder（不用 cmd.exe）
  - Pod 不存在时给友好提示（"实例可能已停止"），不再用过期的存储 Pod 名
  - 空日志返回 "(暂无日志输出)" 而非空字符串（前端 `""` 是 falsy 不渲染）
  - 前端空日志/错误信息加提示文字和背景色
  - 手动 `kubectl scale deployment proj-1782825767454 --replicas=1` 恢复 Pod
  - 更新数据库：status=RUNNING, k8s_pod_name=新的 Pod 名
- **提交**: `0f74522`

## 停止状态显示启动按钮 + 重启/启动后日志自适应刷新
- **需求**：停止后按钮应该变成"启动"而非消失；重启后又无法获取日志，需要自适应
- **后端**：
  - 新增 `startInstance(id)` 方法：K8s `scale --replicas=1`，Docker `start`
  - 新增 `POST /api/instances/{id}/start` 接口
  - `collectK8sLogs` 增加 Pod 查找重试（3次×2秒），重启后 Pod 未就绪时自动等待
- **前端**：
  - STOPPED 状态显示 ▶️ 启动（绿色按钮），替代 🔄重启+⏹停止
  - RUNNING/UNKNOWN 状态显示 🔄 重启 + ⏹ 停止
  - 新增 `StartInstanceModal` 启动确认弹窗
  - 重启/启动后日志面板正打开 → 5秒后自动刷新日志
  - 停止后日志面板显示"实例已停止"提示
- **提交**: `278ad8a`

## 停止状态隐藏链接 + 日志显示停止前缓存
- **需求**：停止后不应显示链接（逻辑不通），日志应可查看之前的（给提示）
- **后端**：
  - `getAccessInfo`: STOPPED 状态直接返回 `{ stopped: true }`，不执行任何命令
  - `getContainerLogs`: K8s STOPPED 状态返回 `{ stopped: true }`，让前端用缓存
- **前端**：
  - 展开面板：STOPPED 显示黄色"实例已停止"提示条，不显示链接
  - 日志区域始终显示（独立于链接是否存在）
  - 新增 `logsCache` state：成功获取日志时按 instanceId 缓存
  - 停止后：有缓存 → 显示缓存日志 + 黄色警告条；无缓存 → 显示"无缓存日志"提示
  - 日志三种显示状态：正常(success) / 停止(stopped+缓存) / 错误(failed)
- **提交**: `5d238e1`

## 修复日志区域不渲染
- **问题**：用户反映"前端日志依旧没有"
- **根因**：日志区域渲染条件是 `{accessInfo && !accessLoading && (...)}`，当 `accessInfo` 为 `null` 时（API 调用失败或边缘情况），日志区域不渲染
- **修复**：改为 `{!accessLoading && (...)}`，只要加载完成就显示日志区域，独立于 `accessInfo`
- **额外**：`handleViewLogs` 添加 `console.log` 调试日志，用户可在浏览器 F12 Console 查看 API 返回
- **注意**：后端当前未运行（port 8080），用户需在 IDEA 重新启动后端
- **提交**: `1752811`

## 日志查看拆分为后台日志和前台日志
- **需求**：日志分开，查看后台日志 + 查看前台日志
- **架构理解**：每个实例是单容器部署（前端打包进后端 JAR），因此：
  - 后台日志 = 容器运行时日志（kubectl/docker logs，Spring Boot stdout）
  - 前台日志 = 构建流水线日志（含 npm install/build + mvn package + docker build + k8s deploy）
- **后端**：
  - `InstanceController` 注入 `BuildService` + `BuildRepository`
  - 新增 `GET /api/instances/{id}/build-logs`：取项目最新构建日志（三级回退：内存→文件→DB）
- **前端**：
  - `api.js` 新增 `getInstanceBuildLogs(id)`
  - `InstanceList.jsx` 日志区域改为双按钮入口：
    - 未查看时：「🖥 后台日志」+「🌐 前台日志」两个按钮
    - 查看中：标签页式切换（高亮当前类型），可随时切换
    - 行数选择器仅在后台日志模式显示
    - 保存文件名区分 backend/frontend
    - 停止/重启/启动后自动刷新仅对后台日志生效
  - 新增 state: `logType`（'backend'|'frontend'）
  - 新增 handler: `handleViewBuildLogs`
- **提交**: `a86ed79`（❌ 方案错误，已回滚）

## 修正：日志按前后台服务动态显示（正确方案）
- **问题**：用户指出"前台日志"不是构建日志，是 O 项目运行时的前端服务日志；"若O项目没有前/后台则省略对应按钮"
- **架构确认**：
  - O 项目是单体部署（Dockerfile 多阶段构建：前端 dist 打包进后端 JAR，一个容器运行 java -jar）
  - K8s 只有一个 Deployment/Pod/容器，运行时只有后端服务（Spring Boot），无独立前端服务
  - 因此 O 项目只有"后台日志"，省略"前台日志"按钮
- **正确方案**：检测实例 Pod 的容器列表，动态显示日志按钮
- **后端**：
  - `InstanceMonitorService` 新增 `getInstanceContainers(id)`：K8s 用 `kubectl get pod -o jsonpath` 解析容器列表，按 name/image 推断角色(frontend/backend)
  - `getContainerLogs` 增加 `container` 参数：`kubectl logs <pod> -c <container>`
- **后端接口**：
  - 新增 `GET /api/instances/{id}/containers`
  - `GET /api/instances/{id}/logs` 增加 `container` 查询参数
- **前端**：
  - `api.js` 新增 `getInstanceContainers(id)`；`getInstanceLogs` 增加 container 参数
  - `InstanceList.jsx` 展开实例时获取容器列表，动态显示按钮：
    - 有 backend 容器 → 「🖥 后台日志」按钮
    - 有 frontend 容器 → 「🌐 前台日志」按钮
    - 单容器（如 O 项目）→ 只显示后台日志
    - 多容器 → 标签页切换前后台
  - 日志缓存按 `id-role` 分开，保存文件名区分前后台
- **提交**: `7148789`

## O 项目改为前后端分离部署（支持前台日志）
- **问题**：用户说"依旧没React日志"——O 项目单体部署只有一个容器，`inferContainerRole` 识别为 backend，前台日志按钮被省略
- **方案**：将 O 项目改为前后端分离部署，在 K8s Pod 中添加 nginx sidecar 容器运行 React 静态文件
- **新增文件**（在 workspace，gitignore 不跟踪）：
  - `Dockerfile.frontend`：多阶段构建，Stage 1 构建前端 dist，Stage 2 nginx:alpine 提供静态文件
  - `nginx.conf`：SPA 路由 + 代理 `/api/` 和 `/ws` 到 `localhost:8080`（同 Pod 后端容器）
  - 修改 `deployment.yaml`：添加 `proj-xxx-frontend` 容器（端口 80），Service targetPort 改为 80
- **Docker 构建**：`DOCKER_BUILDKIT=0 docker build -t proj-xxx-frontend:latest -f Dockerfile.frontend .`
  - build context 是 workspace 根目录，和后端 Dockerfile 相同
- **K8s 部署结果**：Pod 2/2 Running，两个容器
  - `proj-1782825767454`（backend，8080）→ Spring Boot 日志
  - `proj-1782825767454-frontend`（frontend，80）→ nginx 访问日志
  - `inferContainerRole` 识别 `-frontend` 为 frontend 角色 → 前台日志按钮出现
- **BuildService 修改**（代码提交 `45eb418`）：
  - `executeDockerBuildReal`：构建后端镜像后，检测 `Dockerfile.frontend` 存在则自动构建前端镜像
  - `generateDefaultK8sDeployment`：检测 `workspace/frontend/` 目录存在则自动生成两容器 YAML + Service targetPort=80
- **注意**：后端需要在 IDEA 重新编译运行（新增了 containers 接口和 logs container 参数），前端 npm run dev 热更新自动生效

## 添加容器列表接口调试日志
- `getInstanceContainers` 添加 `System.out.println("[CONTAINERS]")` 调试输出
- 前端 `InstanceList.jsx` 添加 `console.log('[容器列表API]')` 调试日志
- **提交**: `7f63486`

## containers 接口返回空数组修复（已回滚）
- **根因**：`kubectl -o jsonpath=...` 在 Windows ProcessBuilder 中可能失败
- **修复**：改用 `kubectl get pod -o json` + Jackson ObjectMapper 解析 `spec.containers`
- **提交**: `c981c4c`（⚠️ 已回滚到 7f63486）

## 前端应用日志收集 React/Vue console（已回滚）
- **需求**：前台日志显示的是 nginx 访问日志，用户要的是 O 项目前端应用本身（React/Vue）的 console 输出
- **方案**：nginx `sub_filter` 注入日志收集脚本 → 拦截 console.log/error/warn → fetch 上报后端 → 内存存储 → UI 展示
- **新增文件**：FrontendLogService.java、FrontendLogController.java
- **修改**：SecurityConfig、InstanceMonitorService、InstanceController、BuildService、api.js、InstanceList.jsx
- **提交**: `4f517c0`（⚠️ 已回滚到 7f63486）

## 回滚操作（2026-07-01 03:12）
- 用户要求回滚，选择回滚到 `7f63486`
- `git reset --hard 7f63486`，撤销 `c981c4c` 和 `4f517c0` 两个提交
- 当前 HEAD：`7f63486`（debug: 添加容器列表接口调试日志）
- ⚠️ containers 接口恢复为旧 jsonpath 实现，可能返回空数组
- 恢复已回滚内容：`git reset --hard 4f517c0`

## 侧边栏数字气泡提示
- **需求**：给左侧边栏所有项（含子菜单）添加数字气泡，显示对应数量
- **后端**：`DashboardService.getDashboardStats()` 扩展，新增 totalInstances/dockerInstances/k8sInstances/totalArtifacts/totalTemplates/totalUsers/totalAuditLogs/totalSchedules
  - 注入 ArtifactRepository/TemplateRepository/UserRepository/AuditLogRepository
  - totalSchedules = pipelines 中 cronExpression 非空且 cronEnabled=true 的数量
- **前端**：`AppLayout.jsx`
  - 新增 `CountBadge` 组件（紫色圆角，count>999 显示 999+）
  - `fetchBadgeCounts` 并行请求 dashboard stats + instances，30秒轮询刷新
  - 所有侧边栏项（含子菜单 全部实例/Docker/K8s）显示对应数量
- **提交**: `3541fc7`
- **注意**：后端需在 IDEA 重启加载新编译的 class
