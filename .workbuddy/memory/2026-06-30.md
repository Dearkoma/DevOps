# 2026-06-30 工作日志

## 建立工作约定
用户阅读项目后明确三条约定：
1. 后续提到"DevOps"即指本项目（DevOps 持续交付平台毕业设计）。
2. 每次对代码操作必须可恢复（git 保障）。
3. 代码修改由用户本人执行，AI 只提供方案。

## 项目状态核查
- 已 git 初始化，最新提交 `f18ef65`（DaoCloud 镜像修复）。
- ⚠️ 当前工作区有大量未提交修改：多个 Controller/Service（Instance、Project、Build、Dashboard、InstanceMonitor、K8sClient）、application.yml、前端多个组件（App.jsx、api.js、AppLayout、AuditLogList、BuildList、InstanceList、PipelineList、ProjectList、SchedulerList、UserManagement）均为 M 状态。
- "可恢复"前提：用户继续改代码前应先处理这些未提交改动（commit 或 stash），否则难以回退到干净基线。已向用户提示。

## 创建基线提交（用户授权 AI 执行）
用户说"这次你帮我提交"，执行基线存档：
- 更新 `.gitignore`：新增忽略 `**/logs/`、`**/cddata/`、`**/data/workspace/`、`*.log`、`*.log.*`、`outputs/`、`参考文献/`。
- `git rm --cached` 停止跟踪运行时日志 `devops-platform/cddata/logs/devops-platform.log`（保留本地文件）。
- `git add -A` + commit，提交 `8d2200f`：33 files changed, +3052/-1570。
- 提交内容：代码改动、前端构建产物、`.workbuddy/memory`。
- 未进库（被 .gitignore 挡住）：日志、`data/workspace/`(107M)、`outputs/`(论文产物)、`参考文献/`(25M PDF)。
- 删除项（用户之前自己删的，本次如实提交删除）：`DevOps平台_IDEA+MySQL运行指南.md`、`DevOps平台使用说明与毕设文档.md`、`毕业设计选题方案.md`。
- 结果：工作区干净，回退基线为 `8d2200f`。

## 侧边栏消失问题诊断
- 用户反馈：访问 `localhost:8080` 后左侧边栏消失。
- 诊断结论：源码正常（`AppLayout.jsx` 第 184 行 `<aside className="sidebar">` 写死渲染，CSS 给了 220px 宽度，无 `display:none`），问题在 `static/assets/` 里的构建产物过时（6/27 13:31 构建，而 `AppLayout.jsx` 在 6/25→现在大改了 278 行）。
- 根因：典型"改前端不 build 导致修改不生效"坑（项目记忆里早有记录）。

## 解答"为何不启动 React 也能进前端"
- 用户疑问：没启动 React，为何访问 Spring Boot 的 8080 能看到前端页面。
- 解释：本项目用 build 模式，`npm run build` 把 React 源码编译成静态文件扔进 `static/`，Spring Boot 内置 Tomcat 自动托管。所以 8080 既是 API 服务器也是前端静态文件服务器，不需要 Vite 开发服务器参与。
- 用 SVG 对比图说明了两种模式：开发模式（npm run dev → 5173/3000，HMR）vs 本项目模式（npm run build → static → 8080 托管）。

## 切换到开发模式
用户要求"完全改成开发者模式"。
- 核查发现 `vite.config.js` 早已配好代理：端口 3000，`/api` 和 `/ws`（含 `ws:true`）转发到 8080。`api.js` 用相对路径 `/api`，两种模式都兼容。
- **结论：无需改任何源码**，只需改用法。
- 给出双终端启动方案：终端 A 跑 Spring Boot（8080），终端 B 跑 `npm run dev`（3000），浏览器访问 3000。
- 提醒：首次访问 3000 需重新登录（localStorage 跨端口不共享）；改前端保存即生效，不再需要 build。
- 保留 `static/` 旧产物不删（可恢复，不影响开发）。
- 已更新 `MEMORY.md` 记录此模式切换。

## 导航栏丢失深度排查（代码层无问题）
用户反馈 8080 和 3000 都丢导航栏。深度排查结论：
- ✅ `AppLayout.jsx`：第 184 行 `<aside className="sidebar">` 写死渲染，无任何条件隐藏
- ✅ `App.css`：`.sidebar { display:flex; width:220px; min-width:220px }`，无 `display:none`、无 media query 隐藏
- ✅ `npm run build` 成功，0 报错，源码能编译
- ✅ `AuthContext`：user 处理 null 安全
- ✅ `ProtectedRoute`：未认证跳 login，认证才渲染 AppLayout
- ✅ `AuthResponse` DTO 有 role 字段；后端 `DataInitializer` 用大写 `ADMIN/MANAGER/DEVELOPER/VIEWER`；前端 `ROLE_LEVEL` 大写 key 匹配
- ✅ `Login.jsx`：`login(data)` 存整个 AuthResponse，user.role 来自后端
- 结论：**源码无任何能导致 sidebar 消失的逻辑**，问题在运行时环境（浏览器缓存 / localStorage 坏数据 / 后端未启动导致 /api/auth/me 失败）
- 排查时发现：用户测试时 8080 后端实际未在跑（curl 000），可能导致 /api/auth/me 失败、user 被置空、菜单被角色过滤清空

## 清空 static 强制开发模式（用户授权 AI 执行 git+构建）
用户要求"完全改成开发者模式"+"只有 React 启动才能访问前后端"：
- `git rm -rf devops-platform/src/main/resources/static/`，删除 3 个构建产物（index.html + 2 个 assets）
- 提交 `d3be63e`：5 files changed, +46/-83
- 8080 不再托管前端，只提供 API；前端必须通过 3000 访问
- 可恢复：`git checkout 8d2200f -- devops-platform/src/main/resources/static/`
- 待用户测试：启动后端+前端，访问 3000，清 localStorage + 强刷 + 登录，验证导航栏

## 定位并修复侧边栏消失根因（用户授权 AI 改代码+构建+git）
用户给出关键线索：F12 里 `<nav class="sidebar-nav"></nav>` 是空的（无子元素）。
- 根因定位：`AppLayout.jsx` 第 155 行 `userLevel = ROLE_LEVEL[user?.role] || 0`，
  当 localStorage 里 `devops_user` 为旧数据/脏数据（role 字段值不匹配 ADMIN/MANAGER/DEVELOPER/VIEWER）时，
  `ROLE_LEVEL[user.role]` 返回 `undefined`，`|| 0` 使 userLevel=0，
  所有菜单项（最低 VIEWER=1）被 `filter` 过滤光，导致 sidebar-nav 为空。
- 修复：改为 `userLevel = ROLE_LEVEL[user?.role] ?? (user ? ROLE_LEVEL.DEVELOPER : 0)`，
  即 user 已登录但 role 无效时降级到 DEVELOPER(2)，保证菜单可见。
- 提交 `dab02c0`：1 file changed, +2/-1
- Vite HMR 自动生效，用户刷新浏览器即可看到菜单
- 彻底解决需用户清 localStorage 重新登录（让后端重新返回正确的 role）
- 可恢复：`git checkout d3be63e -- frontend/src/components/AppLayout.jsx`

## admin 用户管理权限不足修复（用户授权 AI 执行）
用户反馈：admin 登录后访问"用户管理"提示权限不够（403）。
- 后端验证：curl 测 `admin/admin123` 登录 → token role=ADMIN → `/api/users` 返回 200，**后端完全正常**。
- 数据库验证：`users` 表 admin.role='ADMIN'，4 个默认账号 role 都正确。
- 根因：浏览器 localStorage 残留旧账号 token（之前用 developer/viewer 登录过），fetch 带旧 token → 后端按旧 role 判断 → 403。
- 修复：在 `AuthContext.jsx` 引入 `SESSION_VERSION` 机制（当前版本=2）：
  - 启动时检查 localStorage 的 `devops_session_version`，不匹配则清掉 token+user
  - `login()` 时写入新版本号
  - 所有旧会话自动失效，强制重新登录
- 提交 `a531de4`：1 file changed, +20
- Vite HMR 自动生效，用户强刷浏览器即可
- 可恢复：`git checkout dab02c0 -- frontend/src/context/AuthContext.jsx`

## 新建项目自动创建工作目录（用户授权 AI 执行）
用户要求：新建项目后直接点"查看文件夹"，能在不下载的情况下查看对应项目目录。
- 现状：`ProjectService.createProject()` 只写数据库记录，不建工作目录；用户点"查看文件"提示"工作目录尚未创建"
- 改动 `ProjectService.java`：
  - 注入 `devops.pipeline.workspace-dir` 配置，`@PostConstruct init()` 建根目录
  - `createProject()` 调 `createProjectWorkspace()`：建 `<workspace>/<projectCode>/` + 写 README.md（含项目元数据）
  - `deleteProject()` 调 `deleteProjectWorkspace()`：递归删工作目录避免脏数据
  - 工作目录创建/删除失败不抛异常，不影响主流程
- 改动 `ProjectFiles.jsx`：空目录提示从"工作目录尚未创建"改为"目录为空 — 可手动上传或触发构建拉取"
- 编译通过，提交 `bef20af`：2 files changed, +113/-3
- 可恢复：`git checkout a531de4 -- devops-platform/src/main/java/com/devops/platform/service/ProjectService.java frontend/src/components/ProjectFiles.jsx`

## 修复构建失败 - DB schema 与 entity 不一致（用户授权 AI 执行）
用户反馈构建完成后报两个错：
- `service_instances: Field 'environment_id' doesn't have a default value`
- `notifications: Field 'user_id' doesn't have a default value`

根因：DB schema 与 JPA entity 不一致（历史遗留）
- `init.sql` 里 `environment_id BIGINT NOT NULL` + `ON DELETE CASCADE`
- 但 `ServiceInstance.java` entity **没有 environmentId 字段**（改用 projectName 字符串）
- `notifications` 表是 JPA ddl-auto 自动建，`user_id NOT NULL`
- 但 `Notification.java` entity **没有 userId 字段**（改用 recipient 字符串）
- `ddl-auto: update` 只加字段不改约束，老 NOT NULL 约束还在

修复（直接在 MySQL 执行 + 更新 init.sql）：
1. `service_instances.environment_id`: NOT NULL → NULL，CASCADE → SET NULL
2. `notifications.user_id`: NOT NULL → NULL（外键改 SET NULL）
3. 同步更新 `devops-platform/sql/init.sql`，重建库不再出此问题
- DB 验证：`SHOW COLUMNS` 确认两个字段 Null=YES
- 提交 `77273e1`：1 file changed, +2/-2
- ⚠️ DB 改动不可逆，回滚需手动 ALTER 改回 NOT NULL
- 可恢复 init.sql：`git checkout bef20af -- devops-platform/sql/init.sql`

## 确认"D项目"简称
用户要求全面浏览项目代码后，约定后续对话中 **"D项目"** 即指本 DevOps 持续交付平台（毕业设计项目）。已更新 MEMORY.md 记录此简称。
- 代码审查覆盖：17 个 Controller、12 个 Service、12 个 Entity、12 个 Repository、前端 17 条路由 + 15 个组件、四级 RBAC、WebSocket 实时日志、Docker/K8s 集成等全部模块。
