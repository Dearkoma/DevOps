# 2026-07-01 工作日志

## Git 管理约定建立
- 用户要求：每次代码改动自动 git commit，无需询问
- 已将 .workbuddy/ 加入 .gitignore
- 提交了 gitignore 更新 (3d6835a)
- 已建立自动提交的工作约定

## Bug 修复 (提交 f20ec2e)
完成了前后端全面代码审查和关键 bug 修复：

### 前端修复 (9项)
- api.js: 401改用自定义事件替代 window.location.href 全页刷新；添加 30s 请求超时；修复空 body 的 Content-Type 问题
- BuildList.jsx: 修复日志轮询组件卸载后继续运行的内存泄漏 + stale closure + 取消构建无错误处理 + toast 多完成通知覆盖
- InstanceList.jsx: handleDeleteInstance 添加 try/catch，删除失败不关弹窗
- EnvironmentList.jsx: select protectedEnv 类型不匹配 (boolean vs string) 修复 + 名称空值校验
- ProtectedRoute.jsx: 401 跳转保留原始目标路径 (redirect 参数)
- Login.jsx: 登录后支持 redirect 跳转
- AuthContext.jsx: 监听 auth:logout 自定义事件处理 401
- ArtifactList.jsx: 下载 URL 添加安全协议检查
- ProjectList.jsx: handleSave 添加名称和编码空值校验

### 后端修复 (6项)
- SecurityConfig.java: CORS credentials + wildcard 组合修复，使用具体 origin 模式
- BuildService.java: 构建编号 synchronized 保证原子性
- CustomUserDetailsService.java: getEnabled() 空安全比较
- DeploymentService.java: getHistory 全 null 参数 NPE 修复
- DashboardService.java: 分页查询替代全量加载防 OOM
- SchedulerService.java: 手写 Cron 解析器替换为 Spring CronExpression

### 待验证
- 后端 Maven 编译 (进行中)
- 前端 Vite 构建 (进行中)
